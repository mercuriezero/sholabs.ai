import Leads from "@/components/Leads";

export const metadata = {
  title: "Leads · High On AI",
  description: "Your qualified and verified leads across every High On AI growth motion.",
};

export default function LeadsPage() {
  return <Leads />;
}
